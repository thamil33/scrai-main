# PostgreSQL Database Configuration
# Replace 'password' with your actual PostgreSQL password for the 'user' account.
DATABASE_URL="postgresql://user:password@localhost:5432/scrai_db"

# Redis Configuration
REDIS_URL="redis://localhost:6379/0"

# Test Database Configuration (Optional, if you want to run tests on a separate DB)
# Ensure the user has permissions to create and access this database.
TEST_DATABASE_URL="postgresql://user:password@localhost:5432/scrai_db"
TEST_REDIS_URL="redis://localhost:6379/1"
